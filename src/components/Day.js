import React, { useState } from "react";
import "../App.css";

const Day = ({ date }) => {
  //const [value, onChange] = useState(new Date());

  return (
    <div className="">
      <p>date</p>
    </div>
  );
};

export default Day;
